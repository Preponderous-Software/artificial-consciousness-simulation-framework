---
name: Bug report
about: Something in the framework doesn't behave as documented
title: ""
labels: bug
---

**Describe the bug**
A clear description of what's wrong.

**To reproduce**
Steps to reproduce, including the command you ran (e.g.
`python scripts/spawn.py --name "Aria" --provider ollama`).

**Expected behavior**
What you expected to happen instead.

**Environment**
- OS:
- Python version:
- LLM provider (`ollama` / `anthropic` / `openai`) and model:
- Relevant config from `config/default_consciousness.yaml`, if changed:

**Logs**
Relevant excerpt from `~/.consciousness/<name>/run.log`, with any secrets
redacted.
